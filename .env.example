# Copy this file to .env and fill in real values locally.
# NEVER commit the actual .env file — it is already covered by .gitignore.

# Foundation model provider
MODEL_PROVIDER=
MODEL_NAME=
API_KEY=

# Application
APP_ENV=development
LOG_LEVEL=info

# Add tool/service-specific variables below as they are introduced (Week 4+)
